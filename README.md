Під час аналізу `lms-stage-access.log` було знайдено **28 унікальних IP-адрес**.

| Метод | Унікальні IP | Час виконання |
|---|---:|---:|
| Точний підрахунок (`set`) | 28.0 | 3.32041 сек. |
| HyperLogLog | 28.0 | 3.69199 сек. |

HyperLogLog з параметром `p=14` дав майже такий самий результат, як точний підрахунок, з похибкою **0.09%**.

У цьому запуску точний підрахунок виявився трохи швидшим. Це пояснюється тим, що у файлі лише 28 унікальних IP, а основний час витрачається на читання лог-файлу. Для значно більших наборів даних HyperLogLog корисний тим, що використовує менше пам’яті.
