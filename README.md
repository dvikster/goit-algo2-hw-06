Під час аналізу `lms-stage-access.log` було знайдено **28 унікальних IP-адрес**.

HyperLogLog з параметром `p=14` дав майже такий самий результат — **28.0**, з похибкою **0.09%**. Це відповідає очікуваній точності методу.

Час виконання точного підрахунку та HyperLogLog вийшов близьким, бо основний час витрачається на читання лог-файлу. Для більших наборів даних HyperLogLog корисний тим, що використовує менше пам’яті.
